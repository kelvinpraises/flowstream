/**
 * PatternDetector interface definition.
 *
 * Re-exported from types.ts for convenience. This file exists so the
 * patterns/ module has a standalone reference.
 */

export type { PatternDetector, DetectionResult } from "../types.js";
