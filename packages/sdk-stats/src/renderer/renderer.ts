/**
 * VisualRenderer interface — re-exported from adapters for convenience.
 *
 * The VisualRenderer interface lives in adapters/adapter.ts since it's
 * conceptually part of the adapter pattern. This module re-exports it
 * and provides renderer-specific utilities.
 */

export type { VisualRenderer } from "../adapters/adapter.js";
