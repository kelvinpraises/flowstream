/**
 * SDK-specific errors for @flowstream/sdk-stats.
 *
 * Re-exports from @flowstream/types for convenience.
 * Consumers of sdk-stats can import errors directly.
 */

export { ObserverError, AdapterError } from "@flowstream/types";
